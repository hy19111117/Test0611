# 家庭积分管理系统Web后台｜主流暖心UI排版规范（不改动数据库结构）

## 前言

本UI排版方案**完全不修改Supabase任何数据表结构、字段、关联关系、权限逻辑**，仅对 Web 管理后台（Next\.js）的页面布局、元素排版、组件样式、视觉层级、交互体验进行全新升级。适配2026年主流轻量化后台设计，贴合家庭亲子暖心风格，兼顾简约高级、柔和治愈、操作便捷，适配电脑端全尺寸屏幕。

**核心设计定位**：轻量卡片式后台 \+ 暖心治愈风 \+ 极简商务排版，摒弃传统老旧厚重后台样式，适配家庭温馨场景，同时保留管理系统的规整专业性。

## 一、全局通用UI规范（整站统一，所有页面生效）

### 1\. 基础配色体系（暖心主流色系）

沿用项目原有暖橙主色，优化配色层级，弱化刺眼对比，提升柔和质感，适配长时间后台操作：

- **主色调**：暖橙 \#F9943B（温馨亲子、活力不刺眼，保留项目原有品牌色）

- **辅助色**：浅暖黄 \#FFF7E8、浅薄荷绿 \#E8F5F2、浅淡红 \#FEEFEF

- **文字色**：主文字 \#1F2937、次级文字 \#6B7280、占位文字 \#9CA3AF

- **边框/分割色**：极浅灰 \#F3F4F6（无厚重黑线，轻量化主流设计）

- **状态色**：成功绿 \#10B981、待审核黄 \#F59E0B、驳回灰 \#6B7280、危险红 \#EF4444

### 2\. 全局排版规范

- **字体**：系统无衬线字体（Inter \+ 微软雅黑），全页字体统一，无字体错乱

- **标题字号**：页面大标题 20px、模块小标题 16px、卡片标题 14px（加粗）

- **正文字号**：14px（常规内容）、13px（辅助文字、表格内容）

- **行高**：标题 1\.2、正文 1\.6，提升文字可读性

- **圆角统一**：全局大圆角规范化，卡片 16px、按钮 10px、弹窗 20px、输入框 8px（当前主流温柔圆角体系）

- **阴影规范**：仅弱柔和阴影，卡片悬浮浅阴影、常态无阴影，杜绝厚重黑阴影，干净高级

### 3\. 全局布局结构（整站统一模板）

采用主流**左侧固定侧边栏 \+ 右侧自适应主内容区**经典后台布局，适配所有屏幕尺寸：

- 侧边栏：固定宽度 240px，支持收起展开，悬浮高亮、选中背景渐变高亮

- 主内容区：自适应铺满剩余宽度，上下左右统一留白 24px，页面不贴边、不拥挤

- 模块间距：所有卡片、模块统一间距 20px，排版规整有序

- 页面结构固定：页面标题区 → 筛选/操作区 → 数据展示区（卡片/表格）

## 二、公共组件排版规范（所有页面通用）

### 1\. 按钮组件

- **主按钮**：暖橙底色、白色文字、圆角10px、内边距 8px 18px，hover浅亮渐变，用于新增、确认、提交

- **次按钮**：白色底色、灰色边框、灰色文字，hover浅灰底色，用于取消、返回、重置

- **文字按钮**：无底色无边框，主色文字，hover轻微下划线，用于表格编辑、查看操作

- **危险按钮**：浅红底色、红色文字，用于删除、驳回操作

### 2\. 表格组件（核心数据展示）

采用2026主流**轻量化无边框表格**，替代老旧边框表格，干净高级：

- 表头：浅暖灰底色、加粗文字、居中对齐

- 表格行：奇偶行极浅色差，悬浮行高亮底色，无分割黑线

- 单元格：上下内边距 14px、左右 12px，文字居中/自适应对齐

- 状态标签：圆角小标签（8px），对应状态色底色\+浅透明，文字小巧精致

- 操作列：固定宽度，编辑/删除/审核按钮横向均匀排布，间距统一

### 3\. 弹窗表单组件

- 弹窗：圆角20px、浅柔阴影、白色底色，居中弹出，无生硬边角

- 表单布局：标签左对齐、输入框宽度统一，表单项垂直间距 16px

- 输入框：圆角8px、浅灰边框，聚焦主色描边，无刺眼高亮

- 底部按钮区：右对齐排布，主按钮在前、次按钮在后

### 4\. 统计卡片组件（仪表盘核心）

主流暖心卡片设计，替代老旧纯色方块：

- 卡片结构：左侧icon\+标题，右侧核心数据，底部极简小字备注

- 样式：纯白底色、16px大圆角、轻微悬浮阴影

- icon：适配亲子风格彩色emoji/线性图标，柔和不浮夸

- 数据层级：大数字加粗突出，标题次级浅色，层级清晰

## 三、各页面专属排版设计（完全适配原有功能，不改数据库）

### 1\. 仪表盘页面（首页）

布局：**欢迎标题区 \+ 四宫格统计卡片 \+ 左右分栏内容**（当前主流后台首页布局）

- 顶部欢迎区：简约文字问候，搭配微小暖色系装饰线条，温柔治愈

- 统计卡片：4张等宽自适应卡片，均分页面宽度，分别展示总积分、待审核、任务总数、商品总数

- 下半区分两栏：左侧积分排行榜、右侧最近动态记录，两栏等宽排布

- 排行榜优化：前三名金银铜牌柔和底色标注，名次清晰、视觉暖心

- 动态记录：积分加分绿色小字、扣分红色小字，正负状态直观区分，排版紧凑不杂乱

### 2\. 成员管理页面

布局：**顶部标题操作区 \+ 全员数据表格**

- 顶部：左侧页面标题\+功能描述，右侧固定「添加成员」主按钮，布局对称规整

- 表格字段排版：头像（圆形圆角）、姓名、角色、积分、状态、操作，字段均匀分布

- 角色标签：家长/孩子对应不同柔和底色标签，区分清晰

- 状态标签：激活/禁用轻量化状态标识，无厚重样式

- 弹窗表单：适配成员信息录入，输入框统一宽度，排版对齐工整

### 3\. 任务管理页面

布局：**顶部操作区 \+ 任务列表表格 \+ 新增/编辑弹窗**

- 顶部：标题\+新增任务按钮，简洁无冗余

- 表格优化：任务名称、类型、积分、描述、状态、操作，自适应宽度

- 任务类型区分：奖励任务（浅绿标签）、惩罚任务（浅红标签），视觉直观

- 积分数值：加分绿色、扣分红色，数字突出显示

- 状态开关：轻量化圆角开关，替代传统文字状态，更现代化

### 4\. 积分记录页面

布局：**顶部统计卡片 \+ 明细数据表格 \+ 无筛选冗余设计**

- 顶部双卡片：累计加分、累计扣减，暖色系渐变小卡片，简洁醒目

- 表格字段：时间、成员、变动描述、积分、类型，排版从上到下逻辑清晰

- 时间字段统一格式化，对齐规整，无错乱排版

- 数据为空时：暖心空状态提示\+简约图标，替代生硬空白

### 5\. 积分申请页面

布局：**顶部筛选标签栏 \+ 申请列表表格 \+ 审核操作弹窗**

- 筛选区：全部/待审核/已通过/已驳回 圆角标签切换，选中主色高亮

- 表格核心突出：申请人、申请积分、申请理由、状态、操作

- 待审核数据优先视觉突出，方便家长快速处理

- 审核操作：通过（绿色按钮）、驳回（灰色按钮），弹窗支持填写驳回原因，排版整洁

### 6\. 积分商城页面

布局：**顶部操作区 \+ 商品列表表格 \+ 商品编辑弹窗**

- 顶部：标题\+新增商品按钮，布局简洁统一

- 表格字段：商品名称、所需积分、库存、描述、上架状态、操作

- 库存状态：库存充足默认色、库存为0标红「已售罄」，直观提醒

- 上架/下架状态轻量化标签展示，排版统一

### 7\. 兑换记录页面

布局：**顶部筛选栏 \+ 兑换明细表格**

- 筛选逻辑同积分申请页面，样式统一，降低视觉学习成本

- 表格字段：兑换人、兑换商品、消耗积分、兑换状态、操作、时间

- 审核状态清晰区分，支持快速二次核对记录

## 四、细节暖心优化（区别传统后台，提升质感）

- **空状态优化**：所有无数据页面，展示简约插画\+暖心提示文字，不空白、不生硬

- **加载状态优化**：柔和渐变加载动画，替代原生生硬加载

- **hover交互优化**：所有卡片、按钮、表格行均有柔和hover过渡动画，流畅不突兀

- **层级简化**：去除所有多余装饰线、厚重边框、冗余模块，页面干净通透

- **适配优化**：后台自适应电脑大屏、小屏，元素自动缩放排版，无溢出、无错乱

## 五、核心约束说明（严格遵循需求）

1\. **零数据库改动**：所有排版、样式、布局优化，均不修改 family\_members、tasks、point\_records、point\_requests、products、exchange\_requests 任何表结构、字段、数据逻辑、权限策略

2\. **零功能改动**：仅优化视觉排版与交互，原有新增、编辑、删除、审核、统计、兑换所有功能逻辑完全保留

3\. **风格统一闭环**：整站UI风格统一、组件统一、间距统一、配色统一，符合2026轻量化暖心Web后台主流审美，适配家庭积分系统的温馨定位

> （注：文档部分内容可能由 AI 生成）
